# ============================================
# Web Scraper API - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp env.example .env

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://[user[:password]@][host][:port][/database]
DATABASE_URL=postgresql://localhost/tafhub_scrapper
# Alternative examples:
# DATABASE_URL=postgresql://user:password@localhost:5432/tafhub_scrapper
# DATABASE_URL=postgresql://user:password@db.example.com:5432/tafhub_scrapper
# Railway example:
# DATABASE_URL=postgresql://user:password@host.proxy.rlwy.net:port/railway

# ============================================
# Authentication Configuration
# ============================================
# JWT Secret Key - MUST be changed in production!
# Generate a secure random string (at least 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-a-secure-random-string-here

# Google OAuth Client ID (Required for Google Sign-In)
# Get this from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================
# Email Configuration (OTP Verification via Resend)
# ============================================
# Resend is Railway's email service - perfect for production use
# Get your API key from: https://resend.com/api-keys
# Or use Railway's Resend integration: https://railway.app/docs/plugins/resend

# Enable Resend email sending (set to false to log OTPs to console for development)
RESEND_ENABLED=true

# Resend API Key (Required if RESEND_ENABLED=true)
# Get this from Resend dashboard: https://resend.com/api-keys
# Or from Railway's Resend plugin environment variables
RESEND_API_KEY=re_A4icGakc_9LHUKUHc2uD2Nz22qdjPZ248

# Email address to send OTP emails from
# Must be a verified domain in your Resend account
# For Railway's Resend plugin, you can use: noreply@yourdomain.com
# Or use Resend's default domain: onboarding@resend.dev (for testing)
RESEND_FROM_EMAIL=noreply@tafhubscrapper.com

# Legacy SMTP configuration (deprecated - kept for backward compatibility)
# The application now uses Resend API instead of SMTP
# SMTP_FROM_EMAIL can still be used as fallback for RESEND_FROM_EMAIL

# ============================================
# Server Configuration
# ============================================
# Server runs on port 3001 by default (configured in src/main.rs)
# To change the port, modify the bind address in src/main.rs

