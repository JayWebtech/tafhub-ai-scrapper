# ============================================
# Web Scraper API - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp env.example .env

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://[user[:password]@][host][:port][/database]
DATABASE_URL=postgresql://localhost/tafhub_scrapper
# Alternative examples:
# DATABASE_URL=postgresql://user:password@localhost:5432/tafhub_scrapper
# DATABASE_URL=postgresql://user:password@db.example.com:5432/tafhub_scrapper

# ============================================
# Authentication Configuration
# ============================================
# JWT Secret Key - MUST be changed in production!
# Generate a secure random string (at least 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-a-secure-random-string-here

# Google OAuth Client ID (Required for Google Sign-In)
# Get this from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================
# Email Configuration (OTP Verification)
# ============================================
# Enable SMTP email sending (set to true to send real emails)
# If false, OTPs will be logged to console (useful for development)
SMTP_ENABLED=false

# SMTP Server Configuration (required if SMTP_ENABLED=true)
# Example for Gmail:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
# Note: For Gmail, you need to use an "App Password", not your regular password
# Generate one at: https://myaccount.google.com/apppasswords

# Example for SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# Example for AWS SES:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USERNAME=your-aws-smtp-username
# SMTP_PASSWORD=your-aws-smtp-password

# Example for Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USERNAME=postmaster@your-domain.mailgun.org
# SMTP_PASSWORD=your-mailgun-password

# Email address to send OTP emails from
SMTP_FROM_EMAIL=noreply@example.com

# ============================================
# Server Configuration
# ============================================
# Server runs on port 3001 by default (configured in src/main.rs)
# To change the port, modify the bind address in src/main.rs

